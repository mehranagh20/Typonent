# when rgistration ends. --> done
# how much a competition is open. --> done
#
#
# split recieved text --> done
# use binding for txt, bind typed with textarea --> done
# create compete page --> done
# put text and input in the middle. --> done
# parameterize starting line, so that if we change a variable forexample the line start to change at line number 3 not 2. --> done
# prevent select all, copy, paste --> done
# get tab and prevent blur. --> done
# beautify :) --> :| i hate css :(( --> i do really hate css :|
# get text --> done
# start competition --> done
# send info to server --> done
# get other competitors info from server for ranking the --> done
# Scoreboard
# readme, how to contribute
